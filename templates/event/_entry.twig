{% extends "_layout.twig" %}

{% set featureImage = {
  mode: 'crop',
  quality: 90
} %}

{% block content %}
<h1 class="text-4xl text-black font-display my-4">{{ entry.title }}</h1>

<time class="text-sm block pb-4" datetime="{{ entry.postDate | date('Y-m-d') }}">{{ entry.postDate | date('d M Y') }}</time>
<p>{{ entry.eventSummary }}</p>

<h1 class="text-2xl text-black font-display my-4">Artists</h1>

<ul>
  {% for artist in entry.eventArtists.all() %}
    <li>- {{ artist.title }}</li>
  {% endfor %}
</ul>

{# {{ dump(entry.eventBanner.all())}} #}

	{# Output transformed feature image(s) #}
{# {% if entry.eventBanner | length %}
  {% set banner = entry.eventBanner.one() %}
  <img src="{{ banner.getUrl(eventBanner) }}" alt="{{ banner.title }}"/>
{% endif %} #}


{# <img src="{{ image.getUrl(featureImage) }}" alt="{{ image.title }}"/> #}

{# Example from Blog iamge #}
{# {% if entry.featureImage | length %}
  {% for image in entry.featureImage.all() %}
    <img src="{{ image.getUrl(featureImage) }}" alt="{{ image.title }}"/>
  {% endfor %}
{% endif %} #}

<p>{{ someFunction('something') }}</p>


{% endblock %}
